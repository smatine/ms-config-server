server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/abhijit-kalita/config-server-properties-files
          #username: 
          #password: 
          search-paths:
          - app*
          force-pull: true
          
         